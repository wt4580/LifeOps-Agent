"""lifeops.ingest 包说明（给初学者）

这个目录专门负责“把本地文件变成可检索文本”。

你可以把 ingest 理解成知识库的“清洗与入库前工厂”，它做三件事：
1) 扫描文件：递归遍历目录，找出支持的文件类型。
2) 提取文本：按不同文件类型调用不同提取器。
3) 产出标准结构：统一输出 Document / TextChunk，供上层索引写库使用。

当前支持：
- PDF：按页提取文字（保留页码）
- TXT：按 UTF-8 读取全文
- PNG：先图像预处理，再 OCR 识别文字

模块分工：
- scanner.py：总调度器（扫描 + 分发 + 分块）
- pdf_text.py：PDF 逐页文本提取
- txt_text.py：TXT 文本读取
- ocr_png.py：PNG OCR 提取与预处理

为什么要统一成同一种输出结构：
- 上层检索不关心“文本来自 PDF 还是 PNG”，只关心“文本内容 + 来源路径 + 可选页码”。
- 统一结构后，索引、检索、引用展示都更简单、可维护。
"""

